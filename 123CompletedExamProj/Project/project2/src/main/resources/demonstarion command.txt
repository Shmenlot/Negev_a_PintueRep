#demo server name: 
#DemoTopic0
#DemoDataBase0

    #before running

#start kafka
cd kafka_2.12-3.0.0/
zookeeper-server-start.sh config/zookeeper.properties 
kafka-server-start.sh config/server.properties 



#clear mongo
db.users.remove({})

#clear redis
cd redis-6.2.0/src
redis-cli
flushall






    #while presenting

#see mongo files
mongo
use DemoDataBase0
show collections
db.Events.find().pretty()
db.MetaData.find().pretty()

#see redis files
redis-cli
get <key>


    #cleanup
#mongo
--quiet --eval 'db.getMongo().getDBNames().forEach(function(i){db.getSiblingDB(i).dropDatabase()})'
#kafka
./bin/kafka-topics.sh --zookeeper localhost:2181 --delete --topic '*'